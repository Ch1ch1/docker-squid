---
kind: pipeline
type: docker
name: Building..
node:
  drone: ch1

steps:
- name: build & push
  image: plugins/docker
  settings:
    repo: ch1ch1/squid
    tags: [ "5.0.6","latest" ]
    username: ch1ch1
    password:
      from_secret: dockerhub_token
- name: notification
  image: plugins/webhook
  settings:
    urls:
     from_secret: webhookgotify
    content_type: application/json
    template: |
      {
       "message": "{{ repo.owner }} as updated {{ repo.name }} with {{ build.status }}.",
       "priority": 4,
       "title": "{{ repo.name }} build"
      }
  when:
    status: [ success, failure ]
---
kind: signature
hmac: c51be105bd4b96d987ede1e716ee10d209ecf076adf84b44bd32da4dc19c9fe5

...
